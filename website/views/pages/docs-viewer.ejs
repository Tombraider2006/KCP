<style>
/* ===== DOCS LAYOUT ===== */
.docs-main {
    display: grid;
    grid-template-columns: 250px 1fr;
    max-width: 1400px;
    margin: 20px auto;
    padding: 0 20px;
    gap: 40px;
}

.docs-sidebar {
    position: sticky;
    top: 20px;
    height: fit-content;
    background: rgba(20, 30, 50, 0.6);
    padding: 20px;
    border-radius: 8px;
    border: 1px solid rgba(0, 212, 255, 0.3);
}

.docs-sidebar h3 {
    margin-top: 0;
    color: #00d4ff;
    font-size: 1.2em;
}

.docs-sidebar ul {
    list-style: none;
    padding: 0;
    margin: 15px 0 0 0;
}

.docs-sidebar ul li {
    margin: 8px 0;
}

.docs-sidebar ul li a {
    color: #e0e6ed;
    text-decoration: none;
    padding: 8px 12px;
    display: block;
    border-radius: 4px;
    transition: all 0.3s;
    font-size: 0.95em;
}

.docs-sidebar ul li a:hover,
.docs-sidebar ul li a.active {
    background: rgba(0, 212, 255, 0.2);
    color: #00d4ff;
}

/* ===== DOCS CONTENT ===== */
.docs-content {
    background: rgba(20, 30, 50, 0.6);
    padding: 40px;
    border-radius: 8px;
    border: 1px solid rgba(0, 212, 255, 0.3);
    line-height: 1.8;
    color: #e0e6ed;
}

.docs-content h1 {
    color: #00d4ff;
    border-bottom: 2px solid #00d4ff;
    padding-bottom: 15px;
    margin-bottom: 30px;
}

.docs-content h2 {
    margin-top: 40px;
    color: #00d4ff;
    border-left: 4px solid #00d4ff;
    padding-left: 15px;
}

.docs-content h3 {
    margin-top: 30px;
    color: #51a8c9;
}

.docs-content h4 {
    color: #7eb4cb;
}

.docs-content code {
    background: rgba(0, 212, 255, 0.1);
    padding: 2px 6px;
    border-radius: 4px;
    font-family: 'Consolas', 'Monaco', monospace;
    color: #00d4ff;
    font-size: 0.9em;
}

.docs-content pre {
    background: #1e1e1e;
    padding: 20px;
    border-radius: 8px;
    overflow-x: auto;
    border: 1px solid rgba(0, 212, 255, 0.2);
}

.docs-content pre code {
    background: none;
    padding: 0;
    color: inherit;
}

.docs-content table {
    width: 100%;
    border-collapse: collapse;
    margin: 20px 0;
}

.docs-content table th,
.docs-content table td {
    padding: 12px;
    border: 1px solid rgba(0, 212, 255, 0.3);
    text-align: left;
}

.docs-content table th {
    background: rgba(0, 212, 255, 0.2);
    color: #00d4ff;
    font-weight: 600;
}

.docs-content blockquote {
    border-left: 4px solid #00d4ff;
    padding-left: 20px;
    margin: 20px 0;
    color: #a8b4c0;
    font-style: italic;
}

.docs-content a {
    color: #00d4ff;
    text-decoration: none;
    border-bottom: 1px solid transparent;
    transition: border-color 0.3s;
}

.docs-content a:hover {
    border-bottom-color: #00d4ff;
}

.docs-content ul, .docs-content ol {
    margin: 15px 0;
    padding-left: 30px;
}

.docs-content li {
    margin: 8px 0;
}

/* Responsive */
@media (max-width: 768px) {
    .docs-main {
        grid-template-columns: 1fr;
        gap: 20px;
    }
    
    .docs-sidebar {
        position: static;
    }
    
    .docs-content {
        padding: 20px;
    }
}
</style>

<div class="docs-main">
    <aside class="docs-sidebar">
        <h3>📚 Документация</h3>
        <ul>
            <% for (const [id, info] of Object.entries(docs)) { %>
            <li>
                <a href="/docs/<%= id %>" class="<%= docId === id ? 'active' : '' %>">
                    <%= info.title %>
                </a>
            </li>
            <% } %>
        </ul>
        
        <div style="margin-top: 30px; padding-top: 20px; border-top: 1px solid rgba(0, 212, 255, 0.3);">
            <a href="/" style="color: #a8b4c0; font-size: 0.9em; display: block; padding: 8px 12px;">
                ← Вернуться на главную
            </a>
        </div>
    </aside>

    <article class="docs-content">
        <%- content %>
    </article>
</div>

